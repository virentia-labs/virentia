---
"@virentia/vue": minor
---

Add `@virentia/vue` — Vue 3 bindings mirroring `@virentia/react`: `useUnit`, `ScopeProvider`/`provideScope`/`useProvidedScope`, `useModel`, `createModelCache`, and the `component` factory (with `.create()` and controlled models).
